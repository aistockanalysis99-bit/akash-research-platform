"""Options income module — earnings-straddle scanner + paper positions.

Fully additive: separate tables, separate routes, separate scheduler jobs.
Never touches the equity portfolio, its cash balance, or existing pipelines.
"""
